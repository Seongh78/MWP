<form>
    <div class="form-group">
        <!-- 이미지 미리보기 -->
        <div class="posterMainImg" style="text-align: center; margin:15px auto; width:150px; height: 150px; border-radius: 100%; border: 5px dotted #f0f0f0;">
            <label for="imgInp" id="imgLabel" >
            <i class="material-icons" style="font-size:5.5em; margin-top:47px; opacity: 0.35;">add_a_photo</i>
            </label>
            <div style="opacity: 0;">
            <input type='file' id="imgInp" name="projectImgFile"  />
            </div>
        </div>
        <script>
            function readURL(input) {
                if (input.files && input.files[0]) {

                    var reader = new FileReader();
                    var im='<img id="blah" src="#" alt="your image" width="100%" height="100%" style="border-radius: 100%;" />';
                    $('.posterMainImg').html(im);
                    reader.onload = function (e) {
                        $('#blah').attr('src', e.target.result);
                    }
                    reader.readAsDataURL(input.files[0]);
                }
            }

            $("#imgInp").change(function(){
                readURL(this);
            });
        </script>
    </div>


    <br>
    <div class="form-group">
        <label for="exampleInputEmail1">프로젝트명</label>
        <input type="email" class="form-control" id="exampleInputEmail1" aria-describedby="emailHelp" placeholder="이름을 지어주세요">
        <small id="emailHelp" class="form-text text-muted">We'll never share your email with anyone else.</small>
    </div>
    <br>

    <div class="form-group">
        <label for="exampleTextarea">설명</label>
        <textarea class="form-control" id="exampleTextarea" placeholder="간략한 소개를 적어주세요" rows="3"></textarea>
    </div>


    <button type="submit" class="btn btn-primary" style="width: 100%;  padding: 15px;"><i class="material-icons" style="font-size: 1.2em;">loupe</i> 생성</button>
</form>